## Summary

What does this PR change?

## Checklist

- [ ] Ran tests locally (`python -m pip install -e ".[dev]"` then `python -m pytest tests/ -q`)
- [ ] No secrets committed (tokens, passwords, `.env`)

## Notes

Anything reviewers should know (breaking changes, follow-ups)?
